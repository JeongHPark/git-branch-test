let a = 1;
let b = 2;
function addFunc(a, b) {
  return a + b;
}

function minusFunc(a, b) {
  return a - b;
}

// npm init - y => package.json 만들기
// npx tsc --init => tsconfig 만들기
// tsconfig 쪽 파일 주석으로 설명 적어드릴게요.
// 4번 package.json 보시면, scripts에 "npm run dev"
// 5. include / exclude 원하는 파일만 선택해서 컴파일하기기
